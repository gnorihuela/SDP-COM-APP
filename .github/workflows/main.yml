- name: Build APK
        run: |
          # Instalamos flet con soporte para compilación
          pip install "flet[all]" pdfplumber
          # Creamos el entorno de proyecto limpio
          flet create .
          # Compilamos solo para la arquitectura más común de móvil
          flet build apk
